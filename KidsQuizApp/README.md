# لعبة المعلومات – KidsQuizApp 🎓

تطبيق ألعاب تعليمية للأطفال مبني بـ React Native مع Firebase.

---

## 📋 نظرة عامة

**لعبة المعلومات** هي لعبة أسئلة وأجوبة تفاعلية للأطفال تضم:
- **٥٠ مرحلة** بتصاعد في الصعوبة
- **١٠٠٠ سؤال** في مجالات متنوعة (علوم، رياضيات، جغرافيا، تاريخ، حيوانات، رياضة...)
- **نظام نجوم** (١-٣ نجمات لكل مرحلة)
- **لوحة متصدرين** عالمية
- **واجهة عربية** كاملة بتصميم ملوّن ومناسب للأطفال
- **مؤثرات صوتية** وموسيقى خلفية
- **تسجيل الدخول** بالبريد الإلكتروني عبر Firebase

---

## 🗂️ هيكل المشروع

```
KidsQuizApp/
├── App.js                          # نقطة دخول التطبيق
├── package.json
├── firebase.json                   # إعداد Firebase RN
├── firestore.rules                 # قواعد أمان Firestore
├── database.rules.json             # قواعد Realtime Database
└── src/
    ├── config/
    │   ├── firebase.js             # بيانات اعتماد Firebase (يجب تغييرها)
    │   └── gameConfig.js           # ثوابت اللعبة
    ├── constants/
    │   ├── colors.js               # لوحة الألوان
    │   └── strings.js              # النصوص (عربي/إنجليزي)
    ├── context/
    │   └── GameContext.js          # حالة اللعبة العالمية
    ├── data/
    │   └── questions.js            # قاعدة بيانات الأسئلة
    ├── navigation/
    │   └── AppNavigator.js         # التنقل الرئيسي
    ├── screens/
    │   ├── SplashScreen.js         # شاشة البداية
    │   ├── AuthScreen.js           # تسجيل الدخول/الإنشاء
    │   ├── HomeScreen.js           # الشاشة الرئيسية (شبكة المراحل)
    │   ├── GameScreen.js           # شاشة اللعب
    │   ├── ResultsScreen.js        # شاشة النتائج
    │   ├── LeaderboardScreen.js    # لوحة المتصدرين
    │   └── SettingsScreen.js       # الإعدادات
    ├── components/
    │   ├── AnswerButton.js         # زر الإجابة
    │   ├── ProgressBar.js          # شريط التقدم
    │   ├── StageCard.js            # بطاقة المرحلة
    │   └── StarRating.js           # تقييم النجوم
    ├── services/
    │   ├── AuthService.js          # خدمة المصادقة
    │   ├── GameService.js          # خدمة بيانات اللعبة
    │   └── SoundService.js         # خدمة الأصوات
    └── utils/
        └── gameUtils.js            # دوال مساعدة
```

---

## 🚀 الإعداد والتشغيل

### المتطلبات

- Node.js ≥ 16
- React Native CLI
- Android Studio / Xcode
- حساب Firebase

### 1. تثبيت الاعتماديات

```bash
cd KidsQuizApp
npm install
```

#### iOS فقط
```bash
cd ios && pod install && cd ..
```

---

### 2. إعداد Firebase

1. أنشئ مشروعًا في [Firebase Console](https://console.firebase.google.com/)
2. فعّل: **Authentication** (Email/Password) + **Firestore** + **Realtime Database**
3. أضف تطبيق Android وحمّل `google-services.json` → `android/app/`
4. أضف تطبيق iOS وحمّل `GoogleService-Info.plist` → `ios/KidsQuizApp/`
5. عدّل `src/config/firebase.js` بقيمك الحقيقية

---

### 3. نشر قواعد الأمان

```bash
# تثبيت Firebase CLI
npm install -g firebase-tools
firebase login

# نشر قواعد Firestore
firebase deploy --only firestore:rules

# نشر قواعد Realtime Database
firebase deploy --only database
```

---

### 4. إضافة الأصوات

ضع ملفات الصوت `.mp3` بالأسماء التالية:

| الملف | المسار (Android) | المسار (iOS) |
|-------|-----------------|-------------|
| `background_music.mp3` | `android/app/src/main/res/raw/` | `ios/KidsQuizApp/` |
| `correct.mp3` | نفسه | نفسه |
| `wrong.mp3` | نفسه | نفسه |
| `level_complete.mp3` | نفسه | نفسه |
| `button_click.mp3` | نفسه | نفسه |

---

### 5. تشغيل التطبيق

```bash
# Android
npm run android

# iOS
npm run ios
```

---

## 🎮 آلية اللعب

| الإعداد | القيمة |
|---------|--------|
| عدد المراحل | ٥٠ مرحلة |
| أسئلة لكل مرحلة | ٢٠ سؤالاً |
| وقت كل سؤال | ٣٠ ثانية |
| نقاط سهل | ١٠ |
| نقاط متوسط | ٢٠ |
| نقاط صعب | ٣٠ |
| مكافأة السرعة | +٥ إذا أجبت خلال ١٥ ثانية |
| ٣ نجوم | ≥ ٩٠% صحيح |
| ٢ نجوم | ≥ ٧٠% صحيح |
| ١ نجمة | ≥ ٥٠% صحيح |

---

## 🔐 الأمان

- كل مستخدم لا يقرأ/يكتب إلا بياناته الخاصة في Firestore
- لوحة المتصدرين متاحة للقراءة لجميع المسجّلين، وللكتابة فقط لصاحب السجل
- لا يمكن حذف حسابات المستخدمين أو سجلات اللعب

---

## 📦 الاعتماديات الرئيسية

| الحزمة | الإصدار | الاستخدام |
|--------|---------|-----------|
| react-native | 0.72.0 | الإطار الأساسي |
| @react-navigation/native | ^6.1.9 | التنقل |
| @react-native-firebase/* | ^18.7.3 | Firebase |
| react-native-animatable | ^1.3.3 | الحركات |
| react-native-linear-gradient | ^2.8.3 | التدرجات اللونية |
| react-native-sound | ^0.11.2 | الأصوات |
| @react-native-async-storage/async-storage | ^1.21.0 | التخزين المحلي |

---

## 🤝 المساهمة

المشروع مفتوح المصدر. يسعدنا استقبال طلبات التحسين والمراجعات.

---

*© 2024 KidsQuizApp – جميع الحقوق محفوظة*
